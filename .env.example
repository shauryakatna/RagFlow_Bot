# Google Gemini API key (used by the LLM in graph.py)
GOOGLE_API_KEY=

# Path to the document to ingest at startup (.pdf uses PyPDFLoader, else TextLoader)
# A placeholder sample.pdf ships in the repo so it runs out of the box.
DOC_PATH=sample.pdf
